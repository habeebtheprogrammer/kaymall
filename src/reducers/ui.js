import { NEWSLETTER_TOGGLE  } from "../actions/actions"
const initialState = {}

const ui =(state=initialState,action) =>{
    switch (action.type) {

        default:
            return state;
            break;
    }
}
export default ui;